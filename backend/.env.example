DATABASE_URL="file:./dev.db"
PORT=4000
NODE_ENV=development
# Explicitly opt in only on a private local demo. Refused in production.
DEMO_MODE=false
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
# Generate a persistent 32-byte key: openssl rand -hex 32
OTP_ENCRYPTION_KEY=
